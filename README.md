Todo:

- Fix mylist page dropdown arrow +
- Add ticket.png to every sector of rotating wheel +
- Fix fonts and description on page content
- Fetch list results and random wheel values from backend


Feature roadmap:

- Integration with movie api, custom watch lists and marking as watched
- Movie search (also with Russian titles)
- Authentication
- UI improvements (side menu?)
- Movie barcode quiz with leaderboard